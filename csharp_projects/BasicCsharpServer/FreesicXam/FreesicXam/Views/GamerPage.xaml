<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:viewmodels="clr-namespace:FreesicXam.ViewModels"
    x:Class="FreesicXam.Views.GamerPage"
    BackgroundColor="#ff64ff"
    Title="Main">
    <ContentPage.BindingContext>
        <viewmodels:GamerViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Button x:Name="testButton" Text="Test" Clicked="testButton_Clicked" />
        <ListView x:Name="testBox" BackgroundColor="Transparent" ItemSelected="hmmm" 
                  ItemsSource="{Binding Song}">
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" 
                              Detail="{Binding ArtistName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>